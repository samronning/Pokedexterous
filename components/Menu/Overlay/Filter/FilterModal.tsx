import OverlayButton from "../OverlayButton";

const FilterModal = () => {
  return (
    <OverlayButton
      onPress={() => {
        return null;
      }}
      iconName="filter"
      text="Filter"
    />
  );
};
export default FilterModal;
