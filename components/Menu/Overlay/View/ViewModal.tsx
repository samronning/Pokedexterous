import OverlayButton from "../OverlayButton";

const ViewModal = () => {
  return (
    <OverlayButton
      onPress={() => {
        return null;
      }}
      iconName="eye"
      text="View"
    />
  );
};

export default ViewModal;
