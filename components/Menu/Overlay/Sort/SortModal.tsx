import OverlayButton from "../OverlayButton";

const SortModal = () => {
  return (
    <OverlayButton
      onPress={() => {
        return null;
      }}
      iconName='sort'
      text="Sort"
    />
  );
};

export default SortModal;
